/**This file contains mandatory Prefixes for commit messages to ensure commit consistency.
 * To be implemented with github CI/CD if there's time
 * Using 'Setup' for all boilerplate code,
 * 'Verify' and 'Redeem' for specific changes in business logic after setup is done
 * Housekeeping commits could be reordered and squashed accordingly if there's time
 */

const Rules = ["Setup", "Housekeeping", "Verify", "Redeem"];
